export declare class StakeTopup {
    constructor(init?: Partial<StakeTopup>);
    topUp: string;
    stake: string;
    locked: string;
    numNodes: number;
    address: string;
    blses: string[];
}
