import { ApiConfigService } from "src/common/api-config/api.config.service";
import { VmQueryService } from "src/endpoints/vm.query/vm.query.service";
import { ProviderConfig } from "./entities/provider.config";
import { NodeService } from "../nodes/node.service";
import { DelegationData } from "./entities/delegation.data";
import { ProviderFilter } from "./entities/provider.filter";
import { Provider } from "./entities/provider";
import { ApiService } from "@sravankumar02/sdk-nestjs-http";
import { CacheService } from "@sravankumar02/sdk-nestjs-cache";
import { IdentitiesService } from "../identities/identities.service";
import { ProviderQueryOptions } from "./entities/provider.query.options";
import { QueryPagination } from "src/common/entities/query.pagination";
import { ElasticIndexerService } from "src/common/indexer/elastic/elastic.indexer.service";
import { ProviderAccounts } from "./entities/provider.accounts";
export declare class ProviderService {
    private readonly cachingService;
    private readonly apiConfigService;
    private readonly vmQueryService;
    private readonly nodeService;
    private readonly apiService;
    private readonly identitiesService;
    private readonly elasticIndexerService;
    private readonly logger;
    constructor(cachingService: CacheService, apiConfigService: ApiConfigService, vmQueryService: VmQueryService, nodeService: NodeService, apiService: ApiService, identitiesService: IdentitiesService, elasticIndexerService: ElasticIndexerService);
    getProvider(address: string): Promise<Provider | undefined>;
    getProviderAvatar(address: string): Promise<string | undefined>;
    private getNodesInfosForProvider;
    getProvidersWithStakeInformation(): Promise<Provider[]>;
    getProvidersWithStakeInformationRaw(): Promise<Provider[]>;
    private isIdentityFormattedCorrectly;
    getProviders(filter: ProviderFilter, queryOptions?: ProviderQueryOptions): Promise<Provider[]>;
    getDelegationProviders(): Promise<DelegationData[]>;
    getDelegationProviderByAddress(address: string): Promise<DelegationData | undefined>;
    getDelegationProvidersRaw(): Promise<DelegationData[]>;
    getDelegationProviderByAddressRaw(address: string): Promise<DelegationData | undefined>;
    getAllProviders(): Promise<Provider[]>;
    getAllProvidersRaw(): Promise<Provider[]>;
    getProviderAddressesFromApi(): Promise<Provider[]>;
    getProviderAddresses(): Promise<string[]>;
    getProviderConfig(address: string): Promise<ProviderConfig | undefined>;
    getProviderMetadata(address: string): Promise<{
        name: string | null;
        website: string | null;
        identity: string | null;
    }>;
    getNumUsers(address: string): Promise<number | null>;
    getCumulatedRewards(address: string): Promise<string | null>;
    getProviderAccounts(address: string, queryPagination: QueryPagination): Promise<ProviderAccounts[]>;
    getProviderAccountsCount(address: string): Promise<number>;
    getFilteredProviders(filter: ProviderFilter): Promise<Provider[]>;
    isProvider(address: string): Promise<boolean>;
    private getProviderIdentity;
    private getTotalActiveStake;
    private getNumNodes;
}
