export declare class NetworkConstants {
    constructor(init?: Partial<NetworkConstants>);
    chainId: string;
    gasPerDataByte: number;
    minGasLimit: number;
    minGasPrice: number;
    minTransactionVersion: number;
    gasPriceModifier: string;
}
