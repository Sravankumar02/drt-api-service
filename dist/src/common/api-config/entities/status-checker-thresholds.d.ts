export declare class StatusCheckerThresholds {
    constructor(init?: Partial<StatusCheckerThresholds>);
    tokens: number;
    nodes: number;
    providers: number;
    tokenSupplyCount: number;
    tokenAssets: number;
    tokenAccounts: number;
    tokenTransactions: number;
    nodeValidators: number;
}
